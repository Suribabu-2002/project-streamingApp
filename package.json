{
  "name": "streaming-app-fullstack",
  "version": "1.0.0",
  "main": "api/app.js",
  "type": "module",
  "scripts": {
    "start": "nodemon api/app.js",
    "build": "cd front-end && npm install && npm run build",
    "dev": "concurrently \"npm run start\" \"cd front-end && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write \"**/*.{js,ts,jsx,tsx}\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.0",
    "nodemon": "^3.1.10"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "concurrently": "^8.2.2"
  }
}